//! Shared test fixtures: toy tokenizers and deterministic random-weight
//! checkpoints for every architecture. Lives under `tests/` because toy
//! models are a testing concept, not library API.

pub mod tokenizer;
pub mod toy;
